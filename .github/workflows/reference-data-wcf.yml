name: reference-data-wcf

on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
    - src/Services/ReferenceData/**
    - .github/workflows/reference-data-wcf.yml
  pull_request:
    branches: [ master ]
    paths:
    - src/Services/ReferenceData/**
    - .github/workflows/reference-data-wcf.yml

jobs:
  BuildWindows:
    runs-on: windows-latest

    steps:
      - name: 'Checkout Github Action'
        uses: actions/checkout@master
        
      - name: setup-msbuild
        uses: microsoft/setup-msbuild@v1

      - name: Build and run unit tests
        run: |
          nuget restore src/SharedKernel/src/AW.SharedKernel/AW.SharedKernel.csproj

          cd src/Services/ReferenceData
          nuget restore src/AW.Services.ReferenceData.Core/AW.Services.ReferenceData.Core.csproj
          nuget restore src/AW.Services.ReferenceData.Infrastructure.EF6/AW.Services.ReferenceData.Infrastructure.EF6.csproj -SolutionDirectory ..\..
          nuget restore src/AW.Services.ReferenceData.WCF/AW.Services.ReferenceData.WCF.csproj -SolutionDirectory ..\..
          msbuild.exe src/AW.Services.ReferenceData.WCF/AW.Services.ReferenceData.WCF.csproj /p:Platform=AnyCPU /p:Configuration=Release
