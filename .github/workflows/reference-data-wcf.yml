name: reference-data-wcf

on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
    - src/Services/ReferenceData/**
    - .github/workflows/reference-data-wcf.yml
  pull_request:
    branches: [ master ]
    paths:
    - src/Services/ReferenceData/**
    - .github/workflows/reference-data-wcf.yml

jobs:
  BuildWindows:
    runs-on: windows-latest

    steps:
      - name: 'Checkout Github Action'
        uses: actions/checkout@master
        
      - name: setup-msbuild
        uses: microsoft/setup-msbuild@v1
        
      - name: Restore Packages
        run: |
          cd src
          nuget restore AdventureWorks.sln

      - name: Build and run unit tests
        run: |
          cd src
          nuget restore AdventureWorks.sln
          cd Services/ReferenceData/src/AW.Services.ReferenceData.WCF
          msbuild.exe AW.Services.ReferenceData.WCF.csproj /p:Platform=AnyCPU /p:Configuration=Release
