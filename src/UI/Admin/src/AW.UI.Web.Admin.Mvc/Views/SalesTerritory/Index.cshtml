@{
    ViewData["Title"] = "Sales territories";
    @model IEnumerable<AW.UI.Web.SharedKernel.ReferenceData.Handlers.GetTerritories.Territory>;
}

<div class="container">
    <h2>Sales territories</h2>
    @if (Model.Any())
    {
        <div class="row">
            <table class="table table-striped listView">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Country/region</th>
                        <th>Group</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var territory in Model)
                    {
                    <tr>
                        <td>@territory.Name</td>
                        <td>@territory.CountryRegionCode</td>
                        <td>@territory.Group</td>
                    </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="row">
            THERE ARE NO RESULTS THAT MATCH YOUR SEARCH
        </div>
    }
</div>